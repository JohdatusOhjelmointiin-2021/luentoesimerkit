#include <stdio.h>

int main()
{
    char merkki='a';
    char tokaMerkki='b';
    int x;
    int y;
    //char varaa yhden muistipaikan
    //eli tavun (byte) eli 8 bittia (bits)

    printf("merkki on muistipaikassa=%d\n",&merkki);
    printf("tokaMerkki on muistipaikassa=%d\n",&tokaMerkki);

    printf("x on muistipaikassa=%d\n",&x);
    printf("y on muistipaikassa=%d\n",&y);
    return 0;
}
